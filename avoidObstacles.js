function avoidObstacles(inputArray) {
  var obstacles = {};
  var increment = 2;
  if (inputArray.length === 0) {
    return 1;
  } else {
    var current = 0
    while (current <= 40)
  }
  for (var i = 0; i < inputArray.length; i++) {
    obstacles[inputArray[i]] = true;
  }
  for (var j = 0; j < inputArray.length; j += increment) {

  }
}